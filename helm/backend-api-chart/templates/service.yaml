apiVersion: v1
kind: Service
metadata:
  name: {{ include "backend-api-chart.fullname" . }}
  labels:
    {{- include "backend-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "backend-api-chart.selectorLabels" . | nindent 4 }}
